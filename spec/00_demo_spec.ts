describe('Writing a Spec', () => {
    it('is easy whe you know how!', () => {
        //Given - Arrange
        const a = 10, b = 20;
        //when - Act
        const answer = a + b;

        //then - Assert
        expect(answer).toBe(30);

    });
});